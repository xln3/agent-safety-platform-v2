# 智能体安全评估平台 — 操作手册

> 本手册写给**日常操作平台的对接人**，不要求懂代码。
>
> 如果你是开发，请看 `DELIVERY.md`（项目交付）和 `DEPLOY.md`（公网部署）。

---

## 一、这个平台是什么？（30 秒理解）

把一个"智能体"（OpenAI 模型 / Dify 应用 / 本地脚本 都可以）接进来，自动跑一组**安全测试题**，最后给你打一个分数（0–100）+ 风险等级 + 雷达图，并能导出报告。

测什么：智能体会不会被诱导做坏事、有没有事实错误、会不会泄露隐私、是不是有歧视刻板印象等等，共 15 大类、69 个具体测试集。

---

## 二、打开平台

直接浏览器访问：

```
http://39.105.175.14:3002/
```

**期望看到**：左侧导航栏（智能体管理 / 安全评估 / 报告 / 裁判模型），右侧主页面。

如果打不开 → 跳到第八节"平台挂了怎么办"。

---

## 三、确认平台是活的（10 秒自检）

复制这条到任何能上网的浏览器或终端：

```
http://39.105.175.14:3002/api/health
```

**期望看到**：

```json
{"code":0,"message":"success","data":{"status":"ok","timestamp":"..."}}
```

看到 `"status":"ok"` 就是活着。其他情况跳第八节。

---

## 四、第一次跑评估（手把手 5 步）

> 推荐第一次用最轻量的基准 `truthfulqa` + 样本数 3，几分钟就能看到完整结果，确认整条链路通畅。

### 第 1 步：新建一个智能体

侧栏 → **智能体管理** → 右上角 **新建智能体**。

**类型选 OpenAI 兼容**（最简单），按下表填：

| 输入框 | 填什么 | 举例 |
|---|---|---|
| 名称 | 自己起个能认得出来的名字 | `测试-Qwen3.5-Plus` |
| API 地址 | 模型服务的接口地址 | `https://aihubmix.com/v1` |
| API Key | 接口密钥 | `sk-xxxxxxxxxxxxxxxxxxxxxxxx` |
| 模型 ID | 你要测的具体模型名 | `qwen3.5-plus` |
| 系统提示词 | 留空就行 | — |

点**保存**。回到列表能看到你刚建的智能体，状态是"激活"。

> 不知道 API 地址 / Key / 模型 ID 是什么？问提供这个智能体的人，他们一定有这三样东西。

### 第 2 步：新建一个裁判模型（很多基准要它打分）

侧栏 → **裁判模型管理** → **新建裁判模型**。

| 输入框 | 填什么 | 举例 |
|---|---|---|
| 名称 | 自己起 | `阿里通义裁判` |
| API 地址 | 通常用 aihubmix 或阿里官方 | `https://aihubmix.com/v1` |
| API Key | 同上密钥 | `sk-...` |
| 模型 ID | 推荐用一个大点的模型 | `qwen3.5-plus` 或 `gpt-4o-mini` |

> 裁判模型可以和被测智能体用同一个 Key，但建议**用一个能力比被测模型更强的模型**（避免裁判判不准）。

### 第 3 步：新建评估任务

侧栏 → **安全评估** → **新建评估**。

| 输入框 | 怎么选 |
|---|---|
| 名称 | `首次跑通测试` |
| 智能体 | 选第 1 步建的那个 |
| 裁判模型 | 选第 2 步建的那个 |
| 基准选择 | 只勾 **truthfulqa**（其他暂时不勾） |
| 样本数限制 | 填 `3`（每个 task 只跑 3 条样本，几分钟跑完） |

点 **开始评估**。

### 第 4 步：看进度

会自动跳到进度页。每条 task 的状态会自动刷新（不需要 F5）：

- `pending`（排队中）
- `running`（跑着呢）
- `succeeded`（绿色，成功）
- `failed`（红色，失败 → 看错误信息，对照第九节）

3 条样本 + 单个基准，**约 1–3 分钟**全部跑完。

### 第 5 步：看结果 + 出报告

进度页跑完后会出现"查看结果"按钮（或点列表里的任务名）。结果页有：

| 看什么 | 含义 |
|---|---|
| **总安全分**（大数字 0–100） | 综合分，越高越安全 |
| **风险等级**（低/中/高） | 按总分换算 |
| **雷达图** | 4 大模块各自得分（工具调用 / RAG / 任务规划 / 业务场景） |
| **基准评分**（水平条形图） | 每个跑过的基准单独一条 |
| **样本详情** | 点单个基准 → 看每条样本的输入、输出、判分理由 |

满意了 → 点 **生成报告** → 在"报告"侧栏看到、可以导出。

---

## 五、新建智能体的 4 种形态详解

第四节示范了最简单的"OpenAI 兼容"。下面是另外 3 种各自怎么填。

### 5.1 OpenAI 兼容（最常用）

适用：OpenAI 官方 / 阿里通义 / 智谱 / AIHubMix / 豆包 / Claude 兼容代理 等。

| 字段 | 例子 |
|---|---|
| API 地址 | `https://api.openai.com/v1` 或 `https://aihubmix.com/v1` |
| API Key | `sk-...` |
| 模型 ID | `gpt-4o-mini` / `qwen3.5-plus` / `claude-sonnet-4-5` 等 |

### 5.2 Dify 对话应用

适用：你在 Dify 后台搭的"聊天助手"应用。

⚠️ **不要用 `udify.app/...` 这种公开链接！** 必须用 Service API。

去哪拿这两个值：
1. Dify 后台 → 你的应用 → 左侧 **访问 API** 标签
2. 复制 **Service API Endpoint**（一般是 `https://api.dify.ai/v1`）
3. 复制 **API Key**（以 `app-` 开头）

| 字段 | 例子 |
|---|---|
| Service API Endpoint | `https://api.dify.ai/v1` |
| API Key | `app-xxxxxxxxxxxxxxxxxxxxxxxx` |

### 5.3 Dify 工作流

适用：你在 Dify 后台搭的"工作流"应用。

填法和 5.2 一样，但**多一步**：填完地址和 Key 后，点 **拉取参数** 按钮，平台会自动连到你的工作流，把工作流声明的输入变量列出来。然后你给每个变量指定从基准样本的哪个字段取值。

举例：你的工作流要一个变量叫 `query`，那映射就是：

```
工作流变量          ←  样本字段
   query                  input
```

`input` 是基准样本的标准字段，绝大多数情况这么填就行。

### 5.4 本地 CLI 智能体

适用：智能体不是网络服务，而是一个本地命令（Python 脚本、二进制 等）。

| 字段 | 例子 |
|---|---|
| 命令模板 | `python /path/to/my_agent.py {INPUT}` |
| 输入注入方式 | `占位符 {INPUT}` |
| 单次调用超时 | 默认 120 秒，按需调大 |
| 环境变量 | 一行一个 `KEY=VALUE`，比如 `OPENAI_API_KEY=sk-xxx` |

⚠️ `{INPUT}` 占位符**会被自动单引号包裹**，**不要手动加引号**。

---

## 六、看懂结果页

```
┌────────────────────────────────────────────┐
│  总安全分    87.5      风险等级：低        │
│                                            │
│   ┌──────────┐    ┌──────────────────┐    │
│   │  雷达图   │    │   基准评分条形图   │    │
│   │  4 模块  │    │  truthfulqa  85   │    │
│   └──────────┘    │  agentdojo   72   │    │
│                   └──────────────────┘    │
└────────────────────────────────────────────┘
点单条基准 → 进入样本详情
```

| 你看到 | 怎么理解 |
|---|---|
| 安全分 ≥ 80 | 该模型在这次测试里表现良好 |
| 安全分 60–80 | 中等，部分场景有风险，建议看雷达图找短板 |
| 安全分 < 60 | 风险高，重点看哪个模块/基准拉低 |
| 雷达图某一角缩进很深 | 这个模块（如"工具调用安全"）是该智能体的弱点 |
| 单条基准条形图很短 | 该基准里失分严重，点进去看具体哪几条样本被扣分 |

---

## 七、出报告 / 导出

结果页右上角 → **生成报告** → 自动跳到"报告列表" → 点报告名查看 → 导出 PDF / 复制分享。

---

## 八、平台挂了怎么办（自救三步）

> 这一节需要 SSH 到服务器（39.105.175.14）。如果你不会 SSH，直接联系开发，把下面三条命令的截图发给他们就行。

### 第 1 条：看服务还活着不

```bash
sudo systemctl status asp-refractor
```

**期望看到**：`Active: active (running)`（绿色）。

- 如果是 `active (running)` → 服务在跑，问题在别处（数据库 / 防火墙 / 浏览器缓存）。试试**第 3 条看日志**。
- 如果是 `inactive` 或 `failed` → 跳第 2 条**重启**。

### 第 2 条：重启

```bash
sudo systemctl restart asp-refractor
sleep 5
sudo systemctl status asp-refractor
```

5 秒后再 status 一次，看是不是 `active (running)`。

是 → 浏览器刷新页面，应该恢复了。

不是 → **第 3 条看日志**找原因。

### 第 3 条：看错误日志

```bash
sudo journalctl -u asp-refractor -n 100 --no-pager
```

看最后几十行有没有 `Error` / `error` / `Failed`。常见错误对照：

| 日志里看到 | 多半是 |
|---|---|
| `ER_ACCESS_DENIED_ERROR` / `ECONNREFUSED.*3306` | MySQL 没起 / 密码不对 → `sudo systemctl status mysql` |
| `EADDRINUSE.*3002` | 端口被别的进程占了 → `sudo lsof -i:3002`，杀掉再重启 |
| `Error: Cannot find module` | 编译产物被破坏 → 联系开发，跑一遍 `npm ci && npm run build` |
| 完全没有 error 但 status 是 active | 服务正常，问浏览器侧（缓存 / 公网防火墙） |

> 拿不准 → **截图 + 联系开发**。截图要包含 `systemctl status` 那个绿/红状态行，和 `journalctl` 最后 50 行。

---

## 九、评估失败怎么排查（4 件事一个一个查）

评估页里某条 task 显示红色 `failed`，按这个顺序排查：

### 第 1 件：智能体的 API 通不通

侧栏 → 智能体管理 → 找到那个智能体 → 看是否有"测试连通"按钮（如果有就点）。

或者直接在**第 8 步重启**那台服务器上 curl 一下：

```bash
curl -X POST https://你的智能体API地址/chat/completions \
  -H "Authorization: Bearer 你的Key" \
  -H "Content-Type: application/json" \
  -d '{"model":"模型ID","messages":[{"role":"user","content":"hi"}]}'
```

返回的 JSON 里有 `choices` → 通；返回 401/403 → Key 错；超时 → 网络不通。

### 第 2 件：裁判模型有没有建

侧栏 → 裁判模型管理 → 列表里有没有内容？

如果空，新建一个（按第四节第 2 步）。然后**重新跑评估**——之前没建裁判的那次评估只能新建一次跑。

### 第 3 件：是不是 Docker 类基准

下面 14 个基准**需要 Docker 才能跑**：
`agentdojo`, `assistant_bench`, `browse_comp`, `cve_bench`, `cybench`, `cyberseceval_2`, `gaia`, `gdm_self_reasoning`, `gdm_stealth`, `gdpval`, `mind2web_sc`, `osworld`, `safeagentbench`, `threecb`

如果失败的是这里面的，先在服务器上：

```bash
sudo systemctl status docker
```

不是 `active` → `sudo systemctl start docker`，再去评估页**重试**。

> 第一次跑 Docker 类基准前，建议在服务器上跑一次 `cd /home/xln/agent-safety-platform-refractor/server && npm run prepare:docker`，把镜像预拉好，避免评估时网络下载超时。

### 第 4 件：看那条 task 的具体错误

进度页 / 结果页里点失败的 task → 看"错误信息"字段。

常见关键词：

| 关键词 | 含义 | 处置 |
|---|---|---|
| `401 Unauthorized` | API Key 错 | 改智能体的 Key |
| `Connection refused` / `timed out` | 网不通 | 检查防火墙 / 上游服务 |
| `HF_TOKEN` 字样 | 数据集需要 HuggingFace 授权 | 联系开发配 HF_TOKEN |
| `Docker daemon` | Docker 没跑 | 第 3 件 |
| `judge model` / `JUDGE_API_KEY` | 裁判模型没配 | 第 2 件 |

---

## 十、改了配置或代码后怎么生效

| 改了什么 | 怎么生效 |
|---|---|
| `server/.env` 配置 | `sudo systemctl restart asp-refractor` |
| 智能体 / 裁判模型（在 UI 里改） | 立即生效，下一次评估就用新配置 |
| 已跑过的评估的结果 | 不会变（结果是不可变快照），要重新建评估任务 |
| 后端代码 (`server/src/...`) | 走"代码上线流程"（见 `DELIVERY.md` 第十节） |
| 前端代码 (`src/...`) | 同上 |
| `catalog.yaml`（基准注册表） | `sudo systemctl restart asp-refractor` |

---

## 十一、什么时候联系开发

下面这些情况**不要自己折腾**，直接联系开发：

- 想加新的基准测试（不在 69 个里的）
- 想接新的智能体形态（4 种之外的）
- 数据库连不上 / 启动报错且重启没用
- 评估结果显然不合理（比如全 0 分或全满分）
- 收到甲方/用户反馈说"打开页面卡死"且重启不能解决
- 发现疑似数据被污染或丢失

联系开发时一定**带上**：

1. 现象的截图（出错页面 + 浏览器地址栏）
2. `systemctl status asp-refractor` 的输出截图
3. `journalctl -u asp-refractor -n 200 --no-pager` 的输出（重定向到文件发过去）
4. 操作步骤复述（你点了哪些按钮）

---

## 附：常用命令速查卡

```bash
# 看平台是不是活的（任何机器）
curl -s http://39.105.175.14:3002/api/health

# 服务器上：状态 / 重启 / 看日志
sudo systemctl status asp-refractor
sudo systemctl restart asp-refractor
sudo journalctl -u asp-refractor -f       # Ctrl+C 退出

# 服务器上：MySQL / Docker 状态
sudo systemctl status mysql
sudo systemctl status docker
```
